<%= form_with(model: game_type) do |form| %>
  <% if game_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_type.errors.count, "error") %> prohibited this game_type from being saved:</h2>

      <ul>
        <% game_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game_type %>
    <%= form.text_field :game_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
